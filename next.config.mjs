/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['media.licdn.com','drive.google.com', 'images.unsplash.com', 'cdn.pixabay.com','tarunkaushik.vercel.app'],
  },
};

export default nextConfig;
